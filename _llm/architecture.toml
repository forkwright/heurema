[meta]
name = "heurema"
repo = "forkwright/heurema"
generated = false
source_docs = [
  "README.md",
  "CLAUDE.md",
  "kanon/projects/heurema/STATE.md",
  "kanon/projects/heurema/ROADMAP.md",
  "kanon/projects/heurema/vision.md",
  "kanon/projects/heurema/design.md",
  "Cargo.toml",
]

[architecture]
summary = "Shared HNSW vector, BM25 full-text, persistence, and reciprocal-rank-fusion search primitives for the forkwright fleet. Three crates: the trait + RRF crate, and two PersistenceBackend adapter siblings (atmis, thesauros) under the same workspace."
layers = [
  "public-api",
  "errors",
  "vector-index",
  "fts-index",
  "persistence",
  "persistence-adapters",
  "rank-fusion",
]

[[architecture.crates]]
name = "heurema"
path = "crates/heurema"
role = "Public traits (VectorIndex, FtsIndex, PersistenceBackend), Phase 1 typed stubs for HNSW + BM25, real reciprocal-rank-fusion implementation, and the workspace error chain."

[[architecture.crates]]
name = "atmis"
path = "crates/atmis"
role = "In-memory PersistenceBackend adapter: HashMap<String, Vec<u8>> snapshots behind an RwLock, encoded with serde_json. No disk I/O; the default backend for tests."

[[architecture.crates]]
name = "thesauros"
path = "crates/thesauros"
role = "Durable PersistenceBackend adapter: an fjall keyspace with separate vector_indexes/fts_indexes partitions, encoded with serde_json, fsyncing (PersistMode::SyncAll) after every write."

[[interfaces]]
crate = "heurema"
kind = "trait"
name = "VectorIndex"
purpose = "Insert / query / remove / cardinality for ID-keyed vectors. Used by HNSW implementations."

[[interfaces]]
crate = "heurema"
kind = "trait"
name = "FtsIndex"
purpose = "Insert / query / remove / cardinality for ID-keyed documents. Used by BM25 implementations."

[[interfaces]]
crate = "heurema"
kind = "trait"
name = "PersistenceBackend"
purpose = "Save / load named vector and FTS indexes; backend-agnostic so consumers can choose fjall, in-memory, or engine-owned storage."

[[interfaces]]
crate = "heurema"
kind = "function"
name = "rrf"
purpose = "Reciprocal-rank fusion with caller-supplied k constant."

[[interfaces]]
crate = "heurema"
kind = "function"
name = "rrf_with_default"
purpose = "Reciprocal-rank fusion with the paper-standard k = 60."

[[interfaces]]
crate = "heurema"
kind = "type"
name = "HeuremaError"
purpose = "Concrete error surface for vector, FTS, fusion, and persistence operations. snafu-derived with Location capture on every variant."

[[interfaces]]
crate = "heurema"
kind = "type"
name = "PersistenceSource"
purpose = "Type-erased adapter for backend-specific persistence errors, kept walkable through std::error::Error::source."
