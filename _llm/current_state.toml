[meta]
name = "heurema"
repo = "forkwright/heurema"
generated = false
source_docs = [
  "kanon/projects/heurema/STATE.md",
  "kanon/projects/heurema/ROADMAP.md",
  "kanon/projects/heurema/vision.md",
  "README.md",
]

[state]
summary = "Phase 1 ships the public API surface, the real reciprocal-rank-fusion implementation, and typed stubs for HNSW + BM25. Phase 3 ships both PersistenceBackend adapters (atmis, thesauros), unblocked by Phase 2. Consumers can build against the trait shape today; HNSW + BM25 bodies extract from aletheia/krites in Phase 2; persistence is real now."
current_phase = "Phase 1 API/RRF locked; Phase 3 persistence adapters implemented; Phase 2 HNSW/BM25 gated on krites clean-room"
updated = "2026-08-15"

[[recent]]
date = "2026-08-15"
item = "atmis and thesauros implement PersistenceBackend; the trait's save/load methods gained Serialize / DeserializeOwned bounds on I (persistence.rs) since VectorIndex/FtsIndex expose no constructor a generic load could otherwise build I from."

[[recent]]
date = "2026-05-28"
item = "Extracted from kanon/crates/heurema to forkwright/heurema per kanon planning D-067 (Tier B EXTRACT-NOW verdict) + metis-ops #52."

[[recent]]
date = "2026-05-04"
item = "Phase 1 API shape locked: VectorIndex, FtsIndex, PersistenceBackend traits plus real RRF; HNSW + BM25 ship as typed stubs."

[[recent]]
date = "2026-04-22"
item = "Project initiated from fleet storage-tier audit; one HNSW + one BM25 across pinax and mneme cuts the duplicate-failure-surface risk."

[[open_threads]]
id = "phase-2-extraction"
title = "Extract HNSW and BM25 from aletheia/krites"
blocked_on = "aletheia Phase 05g standards-compliance work on krites HNSW + FTS modules"
notes = "Phase 1 is not blocked — only the stubs depend on this thread. Phase 3 (persistence) also does not depend on this thread; it landed independently."
