[package]
name = "heurema"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
authors.workspace = true
description = "Shared vector (HNSW), full-text (BM25), persistence, and reciprocal-rank-fusion search primitives for the forkwright fleet."
readme = "../../README.md"
keywords = ["search", "hnsw", "bm25", "rrf", "vector"]
categories = ["data-structures"]

[dependencies]
snafu = { workspace = true }

[lints]
workspace = true

# WHY: maturity flag is read by the kanon substrate registry so consumers can
# tell at a glance which API surfaces are still pre-stabilization. Phase 1
# lands the API + RRF; HNSW + FTS bodies extract from krites in Phase 2.
[package.metadata.kanon]
maturity = "alpha"
since = "2026-05-25"
phase = "1"
phase-description = "scaffold + API + RRF; HNSW/FTS stubbed pending krites extraction"
exit-criteria = "Phase 2 HNSW + BM25 extraction from aletheia/krites; tracked at kanon/projects/heurema/ROADMAP.md"
